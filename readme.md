## HACKER-RANK BELO-TEST

1- Para correrlo local ejecutar primero "docker-compose up -d"
	* el .env utilizado para correr la app es el siguiente:

POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=payments

2- ingresar a localhost:3000/swagger para ver la doc
3- Crear 2 usuarios en la DB antes de empezar a pegarle a los diferentes endpoint